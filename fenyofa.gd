extends Spatial




func _on_Area_body_entered(body):
#	if body.name == "Player":
#		if body.can_slash == true:
#			$AudioStreamPlayer3D.playing = true
#			yield(get_tree().create_timer(2), "timeout")
#			$AudioStreamPlayer3D.playing = false
	pass
